<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fechas pasadas (deshabilitadas) -->
    <item android:state_enabled="false" android:color="@android:color/darker_gray" /> <!-- Gris claro -->
    <!-- Fechas seleccionadas -->
    <item android:state_selected="true" android:color="@color/colorText" />
    <!-- Fechas disponibles (habilitadas) -->
    <item android:color="@color/colorText" />
</selector>